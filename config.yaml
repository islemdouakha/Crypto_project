crypto:
  key_derivation:
    iterations: 200_000   # PBKDF2 iterations (increase security)
    salt_size: 16
  aes:
    mode: GCM             # AES mode: GCM or CBC
    key_size: 32          # AES-256 (32 bytes)
